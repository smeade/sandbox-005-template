== README

This Rails app was generated by a Rails application template.

`template.rb`, included in the root of this project, can be used like so:

        rails new appname -m template.rb -d postgresql

This rails new command will:

* create the app
* install and configure bootstrap-generators
* prep environment for Heroku
* create Heroku app
* deploy to the Heroku app